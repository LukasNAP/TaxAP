# TaxAP — 50-state rollout: decision procedure and status

Goal: extend TaxAP's dashboard from 2 states (NC, GA) to all 50. See `docs/roadmap-50-states.md` for the up-to-date, all-51-jurisdiction status table (source-discovery layer) — this file is the *procedure* for the deeper per-state investigation (A+ matching layer) that has to happen once a state has a real official source, plus the running log of what's actually been confirmed per state.

**Every state is its own independent set of facts about its A+ setup and its real tax law — there is no small set of "state types" to sort them into.** NC, GA, CA, OH, SC, and AL have each turned out different from one another in ways that don't reduce to a shared category: NC's numbering is a synthesized/trusted-assumption scheme; GA needs address-level boundary matching; CA has an undocumented equipment-tax variant; OH is clean at the A+ level but has county transit-authority surcharges layered on top of the base rate; SC needed address matching for a structurally different reason than CA (clean county+incorporated-place split) and had a data-quality bug (a South Dakota row inside the SC-filtered results); AL has both equipment-tax variants and a distinct "police jurisdiction" concept neither other state has shown. A few specific patterns (the equipment-tax-code suffix, the truncated-row-limit trap) have turned out to recur across states and are captured as reusable checks below — but that's different from states themselves falling into types. Don't treat one state's overall findings as predicting another's beyond the specific reusable checks below — everything else about a state is that state's own business and has to be checked on its own.

This file exists so that adding a state is a repeatable *procedure* (the same questions get asked every time), not that states are a repeatable *category* (the same answers apply across states).

## Step 0 — check current status before assuming anything

Before starting a state, check `docs/roadmap-50-states.md` for where it currently sits — that file has the authoritative, most-recently-verified status for every one of the 51 jurisdictions (official source found/verified, machine-readable or not, A+ matching built or not). Don't re-derive this from scratch, and don't trust a summary of it from memory — read the current file.

## Step 1 — does this state need ship-to address matching, or does an A+ tax-body code map directly to a jurisdiction?

This is the one genuinely reusable question, and it has to be answered per-state from real data — never assumed from a state's general reputation for tax complexity, and never inferred from what another state turned out to need. Run this against the live `XATXBD` chain for the state in question (redact nothing needed, tax-body codes aren't sensitive):

```sql
SELECT *
FROM OPENQUERY([SQL03], 'SELECT * FROM OPENQUERY(APLUS, ''SELECT TBTXBOD, TBTXNAM FROM APLUSV8FAQ.XATXBD WHERE TBTXBOD LIKE ''''<STATE_PREFIX>%'''' AND UPPER(TBTXNAM) NOT LIKE ''''%DO NOT USE%'''' AND UPPER(TBTXNAM) NOT LIKE ''''%DONT USE%'''' AND UPPER(TBTXNAM) NOT LIKE ''''%DON''''''''T USE%'''' AND UPPER(TBTXNAM) NOT LIKE ''''%INACTIVE%'''' AND UPPER(TBTXNAM) NOT LIKE ''''%OBSOLETE%'''''')')
ORDER BY TBTXBOD;
```

(Replace `<STATE_PREFIX>` with the state's likely code prefix — e.g. `CA`, `TX`. If nothing comes back, the prefix guess may be wrong; check a broader sample or search `TBTXNAM` for the state name instead.)

**Standing rule: never hand back or run a query whose result set can include a `DO NOT USE`/retired row or a row that actually names a different state, without excluding it first.** The `NOT LIKE '%DO NOT USE%'` family above (same phrasing `server/aplus-connector.mjs`'s NC query already filters on) is a static filter, safe to include in every state's query up front. Wrong-state contamination is different — it can't be predicted before you've seen the state's real `TBTXNAM` values, so:

1. Run the query with no state-name filter first, and eyeball every `TBTXNAM` for a state name that doesn't match.
2. The moment you find one (like South Carolina's `SC3622` = "South Dakota Vermillion"), add it to an explicit `AND TBTXBOD NOT IN ('<code>', ...)` exclusion — same pattern NC's connector uses for `NCUSE`/`NC060XXX` — before running or sharing that query again for that state.
3. Record the excluded code in that state's `docs/states/<code>.md` file so the next query for that state is written pre-excluded, not rediscovered from scratch.

**Don't trust a `FETCH FIRST 300 ROWS ONLY` limit to be complete for a full-state pull** — South Carolina's real result is 345 rows; an earlier 300-row-limited run silently truncated the tail end of the alphabet and would have produced an incomplete picture if not caught. Run unlimited (drop the `FETCH FIRST` clause entirely) or with a generous limit and confirm the returned row count isn't suspiciously round before treating a state's data as fully seen.

**Also check `TBTXNAM` against the code prefix, not just the code itself** — South Carolina's `SC%`-filtered results included one row (`SC3622`) whose `TBTXNAM` says "South Dakota Vermillion." A naive filter that trusts the `TBTXBOD` prefix alone will silently pull in a wrong-state row. Always sanity-check that `TBTXNAM` actually names the expected state for every row before treating a `LIKE '<PREFIX>%'` result as clean.

Look at what comes back and ask:

- **Does the count of distinct tax-body codes roughly match the number of real jurisdictions that actually vary in rate** for this state specifically (counties, cities, special districts — whatever that state's actual sales-tax structure is, which you have to know or look up, not assume)? Does `TBTXNAM` reveal a usable, discoverable convention?
- **Or can jurisdictions vary *within* a single ZIP code** in this state (common with home-rule cities, county option taxes, or dense special-district overlap)? If so, a code-to-jurisdiction mapping alone won't work — real address/ZIP-level matching is needed, the way GA's `ga-boundary.mjs` does it.

This tells you whether address matching is needed. **It does not tell you anything else about the state** — not whether the rate itself is simple, not whether special surcharges exist, not whether the official source has hidden rows. Those are separate, unrelated questions that happen to each have their own answer per state (see the Ohio transit-surcharge finding below for a concrete example of a state that needed no address matching at all and still had a real rate-comparability problem).

**Check for `E`-suffixed equipment-tax variant codes — a confirmed recurring A+ convention, not a one-off.** First seen in California (`CA1034`/`CA1034E`), then confirmed widespread in Alabama (at least four pairs, e.g. `AL7049`/`AL7049E`, `AL7051`/`AL7051E`, `AL9680`/`AL9680E`) — each `E`-suffixed code sits alongside a base code for the same jurisdiction but carries a different (often much lower) rate, presumably for equipment/machinery sales rather than general goods. Before treating a state's full `TBTXBOD` list as "one code per jurisdiction," check for this pattern and decide explicitly how to handle it (most likely: exclude `E`-suffixed codes from general ship-to sales-rate comparison, since they're a different tax category, not a competing rate for the same sale).

**Check for jurisdiction-type variants beyond the basic county/city split — Alabama's "police jurisdiction" (PJ) codes are a concrete example.** Alabama law lets a municipality tax a zone just outside its city limits (a "police jurisdiction," typically at a reduced rate) separately from both the plain unincorporated-county rate and the full in-city rate — a three-way split, not two. Other states may have their own equivalent structural wrinkles that don't show up anywhere else; the lesson isn't "check for PJ specifically," it's "don't assume a state's jurisdiction model only has the two levels (county, city) seen in earlier states — read `TBTXNAM` carefully for anything indicating a third category."

## Step 2 — is A+'s rate actually comparable to the official source's rate?

This is a distinct question from Step 1 and can fail independently of it — a state can be simple on the address-matching question and still be wrong to compare naively, if the two sides aren't measuring the same thing. Ohio is the concrete example: no address matching needed at all, but ~16 counties have a transit-authority surcharge in the real-world rate that may or may not already be baked into A+'s `TBCRATE`. Before building a comparison for any state, ask:

- Does the state have any surcharge, district, or special-category tax that applies to a *subset* of its jurisdictions, layered on top of the base rate?
- If so, does A+'s `TBCRATE`/`TBCLRT1-4` already include that layer, or does the official source's total include something A+'s number doesn't (or vice versa)? This has to be checked with a real query against a known example jurisdiction — there's no way to infer it from the code structure alone.

If A+'s number and the official number aren't measuring the same total, comparing them directly produces a confidently wrong finding, not an "ambiguous" one — the app has no way to know it's comparing apples to oranges unless someone checks this first.

## Step 3 — does a usable boundary/rate-detail source exist, if address matching is needed?

If Step 1 concluded a state needs address matching, check `docs/roadmap-50-states.md` for whether Streamlined Sales Tax already publishes a usable file for it (most states there do, but many of those files turned out to be ZIP archives the current adapter can't read yet — check the actual current status, don't assume "SST member" means "drop-in ready"). If Streamlined doesn't cover the state, check whether that state's own DOR publishes something address/ZIP-level (the way FL publishes XLSX, TX publishes text/HTML, etc.) that could be adapted instead.

## Step 4 — build, following whatever was actually confirmed for this specific state

- **If no address matching is needed:** adapt `server/ncdor-rates.mjs`'s pattern — fetch the official per-jurisdiction rate table, map tax-body codes to jurisdictions using the *confirmed* convention from Step 1 (don't synthesize and trust a numbering scheme the way NC's adapter does, unless there's no other option — read real codes directly when they're available, the way OH's are), validate all expected jurisdictions are present with a plausible rate range for that state.
- **If address matching is needed:** adapt `server/ga-boundary.mjs`'s tiered-matching pattern (exact address → ZIP+4 → ZIP5 → ZIP+4-sub-ranges-in-agreement) against whatever boundary source Step 3 found. Keep the same defensive posture: bail to null/ambiguous rather than guessing when parsing or matching is unclear.
- **If Step 2 found a surcharge/comparability issue:** decide explicitly how to handle it — bake it into the comparison, exclude the affected jurisdictions from findings with an honest exclusion count, or flag them ambiguous. Don't ship a silent apples-to-oranges comparison.
- Either way: aggregate findings by A+ tax body only (never exposing ship-to-level address/customer identity downstream), and report exclusion counts for anything filtered out rather than silently dropping it.

## Investigated so far

**Milestone (2026-08-26, later session): Layer 2 (A+ matching) investigation is now complete for all 51 jurisdictions** — 47 states/DC investigated below, plus DE/MT/NH/OR correctly excluded (no general sales tax exists there, so Layer 2 doesn't apply). "Investigated" here means Step 1/Step 2 have been answered from real data — it does **not** mean "ready to build." The large majority of the 47 hit a real blocker (a placeholder tax body used by a real share of ship-tos, a confirmed live stale rate, an undecoded jurisdiction type, a missing definition) that needs a human decision, documented per-state below and consolidated in `docs/pending-business-decisions.md`. A small set — MD, IN, KY, MI, ME, CT, MA, MS — came back genuinely clean (a single flat statewide rate with no blocking finding), and have since been built and wired into the dashboard (same day, later in this session) via a generic `server/flat-state-aplus.mjs` reconciler. Combined with NC, GA, and NJ, **11 states were built and wired that session.**

**Milestone (2026-08-27): FL, PA, OH, VA, NY, AZ, and AL added via a new generic `server/direct-mapping-aplus.mjs` reconciler** (for "many codes, each mapped directly to one real jurisdiction" states — no address matching needed, but not a single flat statewide code either). This also generalized the dashboard's "Needs attention" inbox for real: a new batch endpoint (`/api/official/findings`, `readAllWiredStateFindings()`) fetches every wired state's comparison on dashboard load, not just when a user opens that state's drawer — previously only NC and GA fed the inbox eagerly; the flat-rate and direct-mapping states' findings were invisible until manually opened. **18 states are now built and wired: NC, GA, NJ, MD, IN, KY, MI, ME, CT, MA, MS, FL, PA, OH, VA, NY, AZ, AL** — 46 real confirmed findings now visible in the shared inbox. A real generic-reconciler bug was found and fixed along the way: a matched official row with a genuinely unresolved (`null`) total was being coerced to a confident 0% via `Number(null)` instead of treated as unmatched - fixed in `direct-mapping-aplus.mjs`, verified it didn't silently affect FL/PA/OH/VA/NY's already-shipped results.

**MO and CO were investigated but deliberately NOT built this pass** - both need their own official-source adapter built first (neither existed before this session and building one revealed real complexity worth a dedicated pass, not a rushed one): MO's ~92 sparse customer-driven codes don't follow any single discoverable numbering convention even after research, and CO's ambiguous multi-rate cities (confirmed already, Lukas decided "monitor assigned code only") still need a real official source connected before any comparison at all. AR and TX were deliberately NOT included despite having a real adapter and no address-matching requirement — AR needs a real city-to-county crosswalk this project doesn't have a safe source for yet, and TX has a genuine same-city-name-across-counties ambiguity.

Each state's full findings live in their own file under `docs/states/` — don't read one state's file as implying anything about another beyond the reusable Step 1/Step 2 questions above.

| State | Address matching needed? | Rate comparability confirmed? | File |
|---|---|---|---|
| NC | No | Yes — confirmed 0/100 county mismatches live (2026-08-25); earlier `NCMK65` vs `NC060` question resolved (unrelated credit-memo code, not a rate conflict) | `docs/states/nc.md` |
| GA | Yes (fully built) | Yes | — (see `HANDOFF.md`; already fully wired) |
| SC | Yes — clean unincorporated-county vs. incorporated-municipality split, plus a straddling-county-line wrinkle | Official source connected (`server/sc-rates.mjs`, validated 46/46 counties + 294 municipalities); matcher attempt stopped safely after confirming defective RFA FIPS/TaxRate attributes, unresolved Charleston/Dorchester geometry, missing explicit programmatic-use terms, and an outbound customer-address privacy decision | `docs/states/sc.md` |
| NJ | No — A+ has exactly one tax-body row for the whole state (`NJ000`), confirmed by both the `XATXBD` prefix pull (1 row) and live ship-to assignments (599 of 600 active NJ ship-tos on `NJ000`, 1 outlier taxed in GA — border-customer pattern, not a bug); NJ's own law has no local-option tax at all, so there's nothing finer to match into | Yes — `TBCBSRT`/`TBCRATE` both `6.625`, all local components `0`, exactly equal to NJ Division of Taxation's current statewide rate; no surcharge/layering found, no scheduled rate change (`TBNRATE=0`) | `docs/states/nj.md` — **built and wired** (`server/nj-rates.mjs`, `server/nj-aplus.mjs`, `/api/official/states/NJ/aplus`); live 2026-08-26 result reconciles 599 `NJ000` + 1 `GA060` exclusion = 600 active ship-tos |
| AL | Yes (partially) — a genuine three-way split (unincorporated county / in-city "CL" / "Police Jurisdiction" reduced-rate zone, out of scope per decision), plus A+'s numeric code IS ADOR's own locality code directly (confirmed live 2026-08-27 - not synthesized), which sidesteps most name-matching risk. A real, broader-than-expected complication: several AL cities (Birmingham, Hoover, Decatur, Dothan, Huntsville, Madison City, ...) span more than one county and either reuse a different locality's own code number or have no single reliable county to combine with — 29 of 106 live codes fall into this bucket and are reported unmatched rather than guessed | **Built and wired (2026-08-27)** via `server/al-aplus.mjs` at `/api/official/states/AL/aplus`, general-sales-rate-only per Lukas's confirmed decision. **26 confirmed live stale rates found** (most A+ under by 1 point — worth checking whether this is one systemic gap, not 26 independent ones). `AL000` (misinput, 101/~500) and equipment/PJ codes excluded per decision | `docs/states/al.md` |
| AZ | Partially — 2 codes (`AZ203`, `AZ3518`) had no obvious official-source counterpart; `AZ203` turned out to be the City of Maricopa (a real name-duplicate with Pinal County, not Maricopa County itself, confirmed live 2026-08-27 using Lukas's own rate breakdown); `AZ3518` (Green Valley) is a genuine Census-designated place with no incorporated-city row to match, still open | **Built and wired (2026-08-27)** via `server/az-aplus.mjs` at `/api/official/states/AZ/aplus`, business code 017 (Retail) confirmed. County rows already include the 5.6% state rate; city rows are summed with their real county (a verified static crosswalk). **7 confirmed live stale/incomplete rates**: Douglas, Casa Grande, Taylor, City of Maricopa (new), Gilbert (new), Surprise (new) | `docs/states/az.md` |
| MO | Yes (or at minimum, no clean code-to-jurisdiction mapping exists) — 92 sparse, customer-driven codes with no discoverable numbering convention (3 to 10 digits, don't match real FIPS), against an official file believed to carry 2,000+ rows | Unclear — plausible (base rate matches, 2 spot-checked totals line up from general knowledge) but not verified against the live official file | `docs/states/mo.md` |
| CO | Yes — harder than GA/SC: a single city (Aurora, Colorado Springs, Monument, Parker, Timnath, Broomfield) can carry several different official rates depending on which county/RTD/CD-district sliver an address is in; A+ has only 57 sparse, customer-driven codes with no single naming convention (two coexisting schemes) and `TBTXNAM` isn't reliable (one code named "Canon City" is priced as unincorporated Fremont County) | Not confirmed — internally arithmetic-consistent, but a live, material mismatch found on Denver (A+ 8.81% vs. current official 9.15%), unresolved | `docs/states/co.md` |
| LA | Not the usual GA/SC question — A+ has only 2 tax-body codes for the entire state (`LA000` flat statewide catch-all covering 346/353 ship-tos at 11%, `LA001` Jefferson-Parish-specific covering 5) against an official source describing 64 parishes plus hundreds of sub-jurisdictions; there's no real per-jurisdiction A+ config to reconcile against for 98% of ship-tos | Not confirmed — sample size of 2 rows is too small to assess, official XLSX not yet fetched | `docs/states/la.md` |
| NY | No — 59 real codes map ~1:1 to real jurisdictions (county or home-rule city), matched by real locality name parsed from Publication 718's 3-column table (flattened, not by column position - see server/ny-rates.mjs); caveat: home-rule cities are smaller than ZIP codes, so re-deriving `SASTXB` from a raw address would need real address matching even though interpreting an already-assigned code doesn't | **Built and wired (2026-08-27)** via `server/ny-aplus.mjs` at `/api/official/states/NY/aplus`. **2 confirmed live stale rates**: Suffolk County (A+ 8.625% vs. official 8.75%), Yonkers City (A+ 8.375% vs. official 8.875%). One confirmed cosmetic typo tolerated ("Niagra" for Niagara, code number cross-validated). Sherrill/Fulton codes confirmed to have zero active ship-tos - dormant, not urgent. MCTD 0.375% surcharge is *not* a comparability problem — already baked into every affected county/city's published total. | `docs/states/ny.md` |
| HI | **RESOLVED 2026-08-27, per Lukas's explicit decision: Hawaii is excluded from the rate-comparison dashboard entirely**, same as DE/MT/NH/OR — its General Excise Tax legally taxes the seller's gross receipts, not the buyer, so there is no real sales tax to compare. No HI adapter should be built. See `server/official-source-registry.mjs`. | N/A — excluded, not compared | `docs/states/hi.md` |
| IA | No — currently flat, live data shows Iowa's real local option tax (1%) is uniform per-county, not city/place-level as earlier research assumed; sits in a separate "special" (type 63) bucket the existing adapter pattern doesn't read, so it's not a config-only drop-in despite being structurally simple | Yes for the 57 counties A+ has configured (exact match); unconfirmed for the other 42 real counties, which have zero A+ tax-body row at all. **Blocking finding: `IA000` covers 55 of 262 active ship-tos (largest bucket in the state), no DO-NOT-USE tag** | `docs/states/ia.md` |
| KS | Yes — confirmed live: the same city genuinely splits across county lines at different rates (Bonner Springs → 3 different codes/rates by county) | Unclear/partial — state component matches, but 51.6% of the official file's active rows use an undecoded alphanumeric jurisdiction type ("79") not in the code's type map; A+ covers only ~9% of real jurisdictions | `docs/states/ks.md` |
| ND | Unclear/moot — A+ has zero per-jurisdiction granularity to match into at all | **No — worse than HI.** The single ND tax body (`ND000`) is 0% on literally 100% of active ND ship-tos, and unlike HI's optional pass-on tax, ND's sales tax isn't legally optional — this looks like an unbuilt setup, not a policy choice | `docs/states/nd.md` |
| SD | Yes — confirmed live via a real same-ZIP example (two Yankton ship-tos, same city+ZIP, different tax bodies at 4.2% vs. 6.2%) | Yes for correctly-coded bodies (11 real cities match exactly); **blocking finding: `SD000` covers 9 of 43 active ship-tos (~21%)**, a 0%-rate placeholder scattered across cities that also have correctly-coded ship-tos | `docs/states/sd.md` |
| VT | Yes — confirmed live; Vermont has no county-level tax at all, so A+'s 3 county-named catch-all codes are informal and one (Orleans Co.) is confirmed wrong for its one ship-to | **No — 2 confirmed live, material stale rates** on populous codes (Essex Junction: A+ 6% vs. official 7% since 2023; Waterbury: A+ 6% vs. official 7% since 2024), plus Orleans Co.'s ship-to being actively overcharged. **Blocking finding: `VT000` covers 9/46 (19.6%) active ship-tos** | `docs/states/vt.md` |
| NM | Mostly no — 18 real codes map 1:1 to a named city/county; one confirmed exception: Rio Rancho straddles Sandoval/Bernalillo counties with two different official rates, A+ has only the Sandoval-side code | **No — confirmed stale.** NM's statutory safety-valve (NMSA 7-9-4) bumped the state GRT rate 4.875%→5.125% effective 7/1/2026; A+'s base is stale for 100% of NM codes. Roswell was already off by 0.375pt even under the old base. `NM000` (0%) is used by 26/88 active NM ship-tos with no real-world 0% equivalent anywhere in NM's schedule. | `docs/states/nm.md` |
| OH, CA, FL | Partially investigated in earlier sessions (OH: no address matching but an open transit-surcharge comparability question; CA: address matching needed, equipment-tax variant found; FL: likely no address matching needed, cleanest state found so far) | See `docs/roadmap-50-states.md` for current source status | **No dedicated file yet — write one before relying on these summaries for real work.** These bullet points are a carried-over summary from an earlier investigation whose detailed write-up was never actually saved; treat them as a lead to re-verify, not a citable finding. |
| GA (live-verified 2026-08-26) | Yes (fully built) | Yes — ran end to end against production for the first time: 2293 matched + 92 unmatched + 19 ambiguous = 2404 active ship-tos, reconciles exactly. 30 of 130 tax-body findings show a real rate difference. **New finding**: 5 GA-ship-to records carry a non-Georgia tax body (`NC060`×4, `NC041`×3, `SC126`×2, `CA1163`, `PA000`) — the already-documented `CMTXBD`/`SASTXB` cross-context pattern, now confirmed live on real GA addresses; excluding these from the GA findings table (not comparing them as if GA-priced) is still unbuilt. | — (see `HANDOFF.md`; results not yet folded back into it) |
| MN | Yes — 40 of 125 live ship-to cities split across multiple different tax bodies (some clearly wrong, e.g. Woodbury on St Paul/Hennepin codes), and Minneapolis (largest concentration) has no dedicated A+ code at all | **No — a real, systemic gap found**, not just unclear: A+'s 7 Twin-Cities-metro-county codes (Anoka/Carver/Dakota/Hennepin/Ramsey/Scott/Washington) show no uniform surcharge, while a third-party rate aggregator cites Hennepin's real combined minimum as exactly 1.00pt higher than A+'s `MN430` total — consistent with a missing seven-county 0.75%+0.25% metro Transportation/Housing tax across the board (needs MN DOR primary-source confirmation). **Blocking finding: `MN000`, a DO-NOT-USE placeholder, is used by 113 of 387 active MN ship-tos (29%)** — larger than AL's AL000 share — scattered across dozens of cities that also have correctly-coded ship-tos, must be understood before any comparison is built. | `docs/states/mn.md` |
| NE | No — A+'s `NE#####` codes are real Census/GNIS place FIPS codes (confirmed against the 2025 Gazetteer), same numbering Streamlined's own city rows use; only 22 real city codes exist, no county-level A+ codes at all (matches the near-absent real county option tax — only Dakota Co. has one, 0.5%) | 20/22 match exactly; `NE02656` (Aurora) correctly 5.5% with no local row; **`NE18580` (Gering) confirmed stale — A+ 7.0% vs. official 7.5% since 2022-10-01**, not a pending change (`TBNRATE=0`). **Blocking finding: `NE000` DO-NOT-USE used by 21/114 active ship-tos (18%)**, same shape as AL000/MN000. **Contamination found: `NEDS000` = "Netherlands No Tax"**, not Nebraska — inert today (0 ship-tos) but must be excluded explicitly. **Official source connected 2026-08-26** — but the original "config-only, expectedCountyCount: 93" claim was wrong on both counts: real active county count is 1 (only Dakota), and the live file's alphanumeric `GL80x` special-district codes required a real parser fix (see `server/sst-rates.mjs`), not just a config line. Verify any future "drop-in" claim by actually running the adapter before trusting it. | `docs/states/ne.md` |
| NV | Not needed for the general case — 17 A+ codes map 1:1 to all 17 real county-equivalents (16 counties + Carson City), confirmed by name in `TBTXNAM`. **New live finding**: a real but currently-dormant wrinkle exists — Nevada Tourism Improvement Districts (TIDs) create street-address-range special (`63`) jurisdictions inside Reno/Sparks/Las Vegas ZIP codes, confirmed via the SST boundary file's 35 `"A"`-type records; ~22 real active ship-tos loosely screen as inside these blocks | Yes — confirmed live, all 17 counties' `TBCRATE` match the current SST rate file exactly. The TID special codes also currently equal their surrounding county's rate exactly (zero incremental effect today), though that's not structurally guaranteed going forward. **Official source connected 2026-08-26**, genuine zero-code-change drop-in — `stateRate` reads as 0 by design (Nevada's SST encoding has no separate state line, each county row already carries the full total). | `docs/states/nv.md` |
| OK | Yes — real cities split across county lines get separate A+ codes with different totals (Oklahoma City has two: `OK5521` Oklahoma-Co.-side, `OK0921` Canadian-Co.-side), and 744 real active special-tax-district codes (SST type `63`) exist statewide with zero A+ representation at all. A real, current SST boundary file exists (`OKB2026Q3JUN10.zip`) and structurally matches GA's 89-column schema exactly (same offsets for state/county/place/special FIPS) — a plausible `ga-boundary.mjs`-pattern starting point, not yet attempted. | **No — confirmed live, material.** `OK0921` (Oklahoma City, Canadian-County side) carries Oklahoma City's pre-2018 local rate (3.875%) instead of the current 4.125% (effective since 2018-01-01) — an 8-year-stale rate sitting on real live traffic (this code + its Oklahoma-Co. counterpart together cover 79 of 272, ~29%, of active OK ship-tos). 6 of 6 spot-checked county-only codes (Blaine/Mayes/Murray/Muskogee/Ottawa/Tulsa) match the current official county rate exactly; 3 of 4 spot-checked city codes match; internal `TBCBSRT+TBCLRT1-4 == TBCRATE` consistency holds on all 71 rows. `OK000` DO-NOT-USE used by only 2/272 (0.7%) — not blocking at this volume. Special-district layering (744 active codes, 0 in A+) is a second, unresolved comparability question, OH-transit-surcharge-shaped. Not safe to build without a human decision on the OKC stale rate. | `docs/states/ok.md` |
| UT | Yes — real, material within-county variation confirmed live (Salt Lake County alone spans 7.45%–8.45% official across its cities); 45 real codes (44 city + 1 apparent bare-county) against 29 real counties, sparse customer-driven coverage like CO/MO/NE. Prefix decodes to real county FIPS (Utah's own odd-sequential alphabetical scheme), but one code (`UT18135` Salem) is grouped under the wrong county's prefix — a real intra-state miscoding, not just cross-state contamination. A real, current SST boundary file exists (`UTB2026Q3MAY11.zip`), structurally GA-compatible (89 columns, same record-type-4 shape) — a plausible `ga-boundary.mjs` starting point. | **No — confirmed live, widespread.** 27 of 45 real codes (60%) show a live stale-rate mismatch (0.1–0.7pt, always A+ below official), inconsistent even within a single county (some Salt Lake County cities already reflect the 2025-07-01 county-rate increase, others don't) — not a simple "refresh this county" fix. No scheduled updates queued (`TBNRATE=0` on all 46 rows). `UT000` DO-NOT-USE used by 64/268 active ship-tos (23.9%) — the single largest tax-body group, same blocking shape as AL000/MN000/NE000. Not safe to build without human decisions on both the placeholder and the staleness. | `docs/states/ut.md` |
| WA | Yes — confirmed live: 24 of 101 real ship-to cities split across ≥2 A+ tax bodies with materially different rates (e.g. Tacoma spans Pierce-Co-Uninc 9.5% / Tacoma-city 10.3% / Tacoma-Un-Area 10.1%, a third jurisdiction tier beyond county+city); A+'s 64 real codes ride directly on WA DOR's own "location code" numbering (confirmed cross-file: boundary file's `L1702` special code matches A+'s `WA1702`), not synthesized. A real, current SST boundary file exists (`WAB2026Q3MAY27.zip`) but is a 90-column schema, not GA's 89 — `ga-boundary.mjs` needs adaptation, not direct reuse. | Not yet confirmed — one of WA's 2 parser blockers is now fixed (2026-08-26: `parseSstRateCsv`'s jurisdiction-code regex now accepts alphanumeric codes like WA's `L####`, fixed while wiring Nebraska; not independently re-verified against WA's own file). Still open: county-type rows include 5 codes (079/081/083/085/087) beyond the real 39-county FIPS range, likely a Sound Transit RTA-boundary split, unconfirmed. Whether A+'s single `TBCLRT1` slot already nets in the RTA/PTBA/TBD special-district layer is unchecked against a real official total. **Blocking finding: `WA000` DO-NOT-USE used by 33/472 (7%) active ship-tos**, same shape as AL000/MN000/NE000/UT000; **`WA3500`, also DO-NOT-USE, covers 2 Longview/Cowlitz-County ship-tos that have no other valid WA code at all** (a real coverage gap, not just an placeholder). No wrong-state contamination and no CMTXBD/SASTXB cross-state leakage found (0/472). | `docs/states/wa.md` |
| AR | No — 66 real codes map directly to a named AR city/county, combining state+county+city into one `TBCRATE` (NC/SC-style); sparse (~15.6% of 424 real jurisdictions), and one unconfirmed straddle risk (Springdale spans 2 counties, A+ has 1 code) | **No — confirmed live.** 8 of 27 spot-checked codes show a real stale local component (28/324 ship-tos); Arkansas's real $2,500 local-tax cap isn't represented in the source at all. **Blocking: `AR000` covers 46/324 (14.2%)**, largest AR bucket | `docs/states/ar.md` |
| WY | Moot for the live book — 100% of active ship-tos (22/22) sit on non-jurisdiction placeholders (`WY000` 0%, `ZTEMP` 0%), zero real per-county codes in use despite 23 real WY counties | Not comparable — no real mapped code exists to diff against official 4–7% county rates; **needs a human decision** on whether 0% is a deliberate policy or an unbuilt setup | `docs/states/wy.md` |
| CA | **Yes — confirmed live, two reasons.** Same city name maps to 2 different codes/rates for incorporated-city vs. unincorporated-county land (6+ examples); A+'s 143 codes cover only ~26% of CA's 541 real jurisdictions | **No — confirmed live, systemic.** 46/143 real codes (32.2%) diverge from CDTFA's current rate, clustering into an LA-County-wide templated-rate error (~305 ship-tos) plus Sonoma/Santa Clara clusters. **Blocking: `CA000` covers 233/1,574 (14.8%)**, no XATXBD definition found | `docs/states/ca.md` |
| TX | Yes — ~63% of active volume (incl. Dallas/Houston/Fort Worth/San Antonio/Austin/El Paso) sits on a bare-city-name code colliding with 2+ real county-qualified official rows; only 25% of codes map cleanly 1:1 | Partially confirmed — 100/235 codes matched exactly; **1 confirmed live stale rate** (TX1421 Midland: A+ 8.0% vs official 8.25%/7.5%, matches neither) | `docs/states/tx.md` |
| FL | No — FL001–FL067 map 1:1 to all 67 real counties, **matched by real county name (not the code number, which is not a reliable alphabetical index — corrected 2026-08-27, see below)**; no city/home-rule layer exists in FL's real tax structure | **No — built and wired (2026-08-27)** via `server/fl-aplus.mjs` at `/api/official/states/FL/aplus`. 5 confirmed live stale county rates: Collier, Flagler, Hamilton, Palm Beach (original 4) plus Marion (new, found by the full reconciler, not the earlier spot-check) | `docs/states/fl.md` |
| PA | No — PA taxes at the county level only; 67 real counties confirmed, only Philadelphia and Allegheny vary from the flat 6% state rate; A+ has no dedicated Allegheny code at all (coverage gap, not an address-matching need) | **No — built and wired (2026-08-27)** via `server/pa-aplus.mjs` at `/api/official/states/PA/aplus`. Philadelphia (`PA001`, 49 ship-tos) at A+ 6% vs. official 8%, with A+'s own data showing a scheduled fix to 8% on 2026-10-01 (matched by name); PA000 compared against the flat statewide base. Allegheny has no A+ code to even reveal a gap | `docs/states/pa.md` |
| OH | No — 77 codes (OH001–OH088) map 1:1 to real Ohio counties by name; no municipal home-rule tax exists in Ohio | **No — built and wired (2026-08-27)** via `server/oh-aplus.mjs` at `/api/official/states/OH/aplus`. The ~16-county transit-authority (RTA) surcharge crosswalk was found and confirmed (special jurisdictionCode `<A+ county number>000`) and folded into the comparison, clearing 10 counties that a naive county-row-only diff would have false-flagged. 4 confirmed live stale rates remain after that: Knox (+0.5pt understated), Portage/Tuscarawas/Brown (all overcharged 0.25-0.5pt, new findings). **`OH000` (implausible 0%, 65/936, 6.9%) treated as a misinput per the standing default, excluded from comparison** | `docs/states/oh.md` |
| TN | Yes — confirmed live (SC-shaped): rates genuinely vary within a county (Memphis 9.75% vs. bare Shelby Co. 9.25%; Gallatin 9.75% vs. bare Sumner Co. 9.25%); 95 real counties match exactly, but sub-county coverage is sparse/customer-driven | **No — 7 confirmed live mismatches** covering 85/929 ship-tos (Nashville/Davidson +0.5pt on 66 ship-tos; a stale McMinn Co. cluster since 2020; Mount Juliet/Spring Hill since 2020; Tipton Co.; Kingsport; Mountain City since 2025). **Blocking: `TN000` (no definition) covers 183/929 (19.7%)**, TN's largest bucket | `docs/states/tn.md` |
| MD | No — exactly one real MD code (`MD000`), covering 99.7% of active ship-tos; matches MD's actual law (local sales tax fully preempted) | **Yes — confirmed live and exact, and now built and wired (2026-08-26)** via `server/flat-state-aplus.mjs` at `/api/official/states/MD/aplus`. `MD000` = 6% matches the Comptroller's current flat rate exactly, no pending change; no blocking finding | `docs/states/md.md` |
| IN | No — one real code (`IN000`) covers 510/514 IN-tagged ship-tos; official source confirms zero county/city/special rows exist for Indiana | **Yes — confirmed live and exact, and now built and wired (2026-08-26)** via `server/flat-state-aplus.mjs` at `/api/official/states/IN/aplus`. `IN000` = 7% matches the official flat rate exactly; no blocking finding (the other 4 codes are cross-context/ISO-collision noise, not IN gaps) | `docs/states/in.md` |
| KY | No — one real code (`KY000`) covers 299/302 ship-tos; official source confirms zero local jurisdictions exist for Kentucky | **Yes — confirmed live and exact, and now built and wired (2026-08-26)** via `server/flat-state-aplus.mjs` at `/api/official/states/KY/aplus`. `KY000` = 6% matches the official flat rate exactly; no blocking finding | `docs/states/ky.md` |
| MI | No — one real code (`MI000`) covers 459/466 MI-tagged ship-tos; Michigan has no local-option layer at all | **Yes — confirmed live and exact, and now built and wired (2026-08-26)** via `server/flat-state-aplus.mjs` at `/api/official/states/MI/aplus`. `MI000` = 6% matches the official flat rate exactly (unchanged since 1994); no blocking finding | `docs/states/mi.md` |
| RI | No — one real code (`RI000`) covers 100% of active RI ship-tos (40/40) and customers (22/22); Rhode Island has no county government or local tax | **No — confirmed live and material.** `RI000` is configured at **0%** vs. Rhode Island's real flat 7% rate, affecting 100% of active RI ship-tos — same shape as ND's finding, **needs an urgent human decision** on whether RI tax is actually being collected today | `docs/states/ri.md` |
| IL | Yes — A+'s `IL#######` codes are IL DOR's own real composite county+place codes (no re-derivation needed once assigned), but a few cities straddle counties with 2 codes each (Aurora, Elgin) — address-dependent | Not yet confirmable — no adapter exists. **Blocking: `IL000`/`ILOOO` cover 454/891 (51.0%)** — the largest placeholder share found in this entire rollout, larger than UT000's prior high | `docs/states/il.md` |
| VA | Mostly no — 106 real `VA###` codes map cleanly by name to 106/133 real localities; but VA has counties and independent cities sharing the same name (Fairfax, Franklin, Richmond, Roanoke), disambiguated by the official workbook's own literal "County"/"City" name suffix (server/va-aplus.mjs), not inferred | **Built and wired (2026-08-27)** via `server/va-aplus.mjs` at `/api/official/states/VA/aplus`. **Richmond confirmed as a real live miscoding** (57 of 59 `VA076` ship-tos carry City-of-Richmond ZIPs, not Richmond County - see docs/pending-business-decisions.md), not just a suspicious volume skew. **1 new confirmed stale rate**: Pittsylvania County (+1.0pt). Two codes unmatched: a confirmed cosmetic typo (Albermarle/Albemarle) and a legacy Bedford-city code no longer a real jurisdiction | `docs/states/va.md` |
| WI | Yes — 49 real county codes map cleanly to 68% of WI's 72 counties (NC-style), but WI's Premier Resort Area municipal surtax has zero A+ representation — full accuracy needs GA-style place matching | Not yet confirmable — no adapter exists (all 49 real codes internally consistent). **Blocking: `WI000` covers 42/424 (9.9%)**, same shape as other placeholder findings | `docs/states/wi.md` |
| WV | Yes (SC/AL-shaped) — WV has no county sales tax at all; every local tax is municipal and can vary within a ZIP; A+'s 27 real codes are correctly assigned today but deriving one from a bare address needs real boundary matching | Not yet confirmable — no adapter exists (all 27 real codes internally consistent, state 6% + uniform municipal 1%). **Blocking: `WV000` covers 21/112 (18.75%)**; also `WV961` is labeled "Missouri Lewisburg" on a live code — cosmetic typo or real miscoding, unconfirmed | `docs/states/wv.md` |
| AK | **RESOLVED 2026-08-27, per Lukas's explicit decision: excluded from the comparison dashboard entirely**, same as Hawaii — Alaska does have real local-only sales tax, but no state tax, and the only public source (ARSSTC) never covers every taxing jurisdiction, so a comparison here could never be complete. No AK adapter will be built. See `server/official-source-registry.mjs`. | N/A — excluded, not compared | `docs/states/ak.md` |
| CT | No — one real code (`CT000`) covers all 181 active CT ship-tos and 75 customers; Connecticut has no county or municipal sales tax at all | **Yes — confirmed live and exact, and now built and wired (2026-08-26).** A new live-fetch adapter (`server/ct-rates.mjs`) parses DRS's own rate sentence and is reconciled via `server/flat-state-aplus.mjs` at `/api/official/states/CT/aplus`. `CT000` = 6.35% matches exactly; no blocking finding | `docs/states/ct.md` |
| DC | No — one real code (`DC000`) covers 20/21 active DC ship-tos (95%); DC has no counties or home-rule cities, a single unified jurisdiction | **No — confirmed live and material.** `DC000` is configured at **0%** against DC's real, legislated (mandatory) rate of ~6.5% today stepping to 7.0% on 2026-10-01 — same ND-shaped finding as RI, **needs an urgent human decision** | `docs/states/dc.md` |
| ID | Unclear — moot for A+'s 19 real codes today (none represent an actual resort-city jurisdiction), but that's because A+ doesn't model ID's real complexity, not because the state is clean | Not yet confirmable — no adapter exists, and Idaho's Tax Commission doesn't centrally publish its ~23 resort-city local rates at all (a Layer 1 gap, not just Layer 2). **Blocking: `ID000` covers 12/95 (12.6%)** | `docs/states/id.md` |
| ME | No — one real code (`ME000`) covers 100% of active ME ship-tos (76/76); Maine has no local-option sales tax at all | **Yes — confirmed live and exact, and now built and wired (2026-08-26).** A new live-fetch adapter (`server/me-rates.mjs`) parses Maine Revenue Services' own rate table (cross-validated against its Use Tax row) and is reconciled via `server/flat-state-aplus.mjs` at `/api/official/states/ME/aplus`. `ME000` = 5.5% matches exactly; no blocking finding | `docs/states/me.md` |
| MA | No — one real code (`MA000`) covers 342/343 active MA ship-tos; Massachusetts' general sales tax has no local-option variation | **Yes — confirmed live and exact, and now built and wired (2026-08-26).** A new live-fetch adapter (`server/ma-rates.mjs`, using a `curl/8.0` User-Agent since mass.gov bot-blocks browser-style fetches) is reconciled via `server/flat-state-aplus.mjs` at `/api/official/states/MA/aplus`. `MA000` = 6.25% matches exactly; no blocking finding | `docs/states/ma.md` |
| MS | No — one real code (`MS000`, flat 7%) covers 267/270 active ship-tos; matches Mississippi's lack of a general local-option tax | **Yes — confirmed live and exact, and now built and wired (2026-08-26).** A new live-fetch adapter (`server/ms-rates.mjs`) parses MS DOR's Retail Sales line item and is reconciled via `server/flat-state-aplus.mjs` at `/api/official/states/MS/aplus`. `MS000` = 7% matches exactly. Open, unresolved comparability gap (not blocking the general comparison): MS DOR separately publishes Jackson (+1%) and Tupelo (+0.25%) city levies with zero A+ representation (`MS_CITY_LEVY_CAVEAT`) | `docs/states/ms.md` |
| All others (DE, MT, NH, OR) | N/A — confirmed no general state or local sales/use tax exists; excluded from comparison entirely, not unbuilt | N/A | See `docs/roadmap-50-states.md` |

When investigating a new state, create `docs/states/<code>.md` following the same structure as the existing files (address matching, rate comparability, boundary source if needed, open questions, do-instead) and add a row to the table above.
